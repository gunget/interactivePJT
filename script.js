(function () {
  // const windowHeight = window.innerHeight;
  // const windowWidth = window.innerWidth;
  // document.body.style.setProperty("--windowHeight", windowHeight);
  // document.body.style.setProperty("--windowWidth", windowWidth);
})();
